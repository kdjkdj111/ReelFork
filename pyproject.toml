[project]
name = "reelfork"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = [
    "requests==2.32.3",
    "notion-client==2.2.1",
    "google-generativeai==0.8.3",
    "pyproj==3.6.1",
]

[tool.vercel]
entrypoint = "api.save:handler"
